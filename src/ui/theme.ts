/** Type scale and spacing; main text ≥16 sp (UX constraints). */
export const type = {
  title: 28,
  body: 16,
} as const;

export const spacing = {
  s: 8,
  m: 16,
  l: 24,
} as const;

export const colors = {
  background: "#FFFFFF",
  text: "#1B1B1F",
  subtle: "#5F6368",
} as const;
