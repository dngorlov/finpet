/**
 * Repository seam anchor — implementations land with M1 (ROADMAP §3.2).
 * The full repository set (profiles, days, plans, transactions, savings,
 * goals, petState, dayScores, taskProgress) is created here, not in
 * screens; M1 fills this directory rather than inventing its layout.
 */
export {};
